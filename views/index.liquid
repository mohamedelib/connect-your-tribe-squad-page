{% render 'partials/head.liquid', title: 'Squad page' %}

<header>
  <img src="/images/fdnd.png" alt="fdnd" width="150" height="150">
</header>
    <main>
      <form>
    <h1>Zoek je squad-mate!</h1>
    <input type="search" placeholder="Zoek..">
        </form>
    <button>filter</button>

    {% for person in persons %}
      <div class="Squadcard">
        <img src="/images/prop.jpg" width="250" height="250" alt="profielfoto van Mo">
        <p>{{ person.name }}</p>
      </div>
    {% endfor %}

  </main>
</body>
{% render 'partials/foot.liquid', squads: squads %}
